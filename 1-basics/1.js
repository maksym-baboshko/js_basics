// Basic arithmetic operators

const width = 10;
const height = 5;
const space = width * height;

const partOfWidth = width - 4;
const newWidth = width + 4;

const division = newWidth / width;
const volume = 2 ** 3; // 2 * 2 * 2

console.log(division);

// Strings

const country = 'Ukraine';
const city = 'Kharkiv';
const fullName = 'Maksym Baboshko';
const age = 26;

// String Concatenation (Command + /)
console.log(country + ', ' + city);
console.log(fullName + ', ' + age);

/* Multiline
   comments (Shift + Option + A) */
